import expect from 'expect'
// import sinon from 'sinon'
import * as makeTree from '../lib/make-tree'

describe('MakeTree', () => {
  it('should exists', () => {
    expect(makeTree).toExist()
  })
})
