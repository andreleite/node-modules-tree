import expect from 'expect'
// import sinon from 'sinon'
import * as getDependencies from '../lib/get-dependencies'

describe('GetDependencies', () => {
  it('should exists', () => {
    expect(getDependencies).toExist()
  })
})
